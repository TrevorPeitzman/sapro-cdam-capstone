import arc from './arc.js';
import pie from './pie.js';

const ag = arc();
// TEST DATA at the moment
const pg = pie()([5,6,7,8,9]);

var testarc = a({
    innerRadius: 0,
    outerRadius: 100,
    startAngle: 0,
    endAngle: Math.PI / 2
});




